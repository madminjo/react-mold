# React Starter – Vite + Tailwind + React Router + Sass

A minimal, production-ready starter with:
- ⚡ Vite
- ⚛️ React + React Router DOM
- 🎨 Tailwind CSS
- 🧩 Sass/SCSS
- 🧠 Context API example
- 📁 Clean folders (components, pages, context)

## 1) Requirements
- Node.js LTS installed (recommend the latest LTS version)
- Git installed

## 2) Create a new project from this template

**Option A: Use this as your own repo**
```bash
# Clone your repository
git clone https://github.com/<your-username>/<your-repo>.git
cd <your-repo>

# Install dependencies
npm install

# Start dev server
npm run dev
# react-mold
